import 'package:flutter/material.dart';

class ProductCategoryView extends StatelessWidget {
  const ProductCategoryView({super.key});

  @override
  Widget build(BuildContext context) {
    return const ProductCategoryBody();
  }
}

class ProductCategoryBody extends StatelessWidget {
  const ProductCategoryBody({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
